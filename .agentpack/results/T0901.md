# T0901 Result

## Outcome

Completed

## Scope delivered

- Replaced the clipped orders table with a focusable, touch-friendly horizontal scroll container, a sticky header, a sticky selection column, and stable minimum column sizing.
- Rebuilt advanced filters as a responsive grid and added exact WooCommerce core statuses plus source, local workflow, export state, governorate, product, category, payment, shipping, POS, author, and date filters.
- Added an account-scoped server-side governorate filter over normalized Woo shipping/billing state and city fields.
- Localized and visually distinguished `never-exported`, `exported`, and `changed-after-export` without adding any remote Woo mutation.
- Made Arabic navigation icon placement deterministic and renamed the operator-facing field-mapping entry to store-data setup terminology.

## Acceptance evidence

| Acceptance criterion | Evidence |
| --- | --- |
| Wide table scrolls without clipping | `TableContainer` is focusable with horizontal overflow; Playwright asserts `scrollWidth > clientWidth` |
| Responsive bounded filters | CSS grid in `App.tsx`; browser test verifies governorate filter reaches `/api/v1/orders/query` |
| Exact Woo and export states | All eight Woo core identifiers and three local export states are rendered and filterable |
| Arabic navigation order and terminology | Playwright geometry asserts the icon is on the visual right; obsolete Arabic label is absent |

## Validation

| Command | Result | Notes/artifact |
| --- | --- | --- |
| `npm run agent:validate` | Pass | 46 tasks |
| `npm run format:check`, `npm run lint`, `npm run typecheck` | Pass | All workspaces |
| `npm run test:unit` | Pass | Includes normalized governorate repository test |
| `npm run test:integration` | Pass | 47 persistence + 23 API integration tests |
| `npm run test:e2e` | Pass | 18 browser + 9 API journeys |
| `npm run test:a11y` | Pass | 5 accessibility journeys |
| `npm run test:visual` | Pass | 2 updated deterministic baselines |
| `npm run build`, `git diff --check` | Pass | Production build and whitespace |

## Files and contracts changed

- Orders UI, shell navigation copy/layout, persistence filter allowlist, repository and browser tests, and visual baselines.
- Added the bounded `governorate` order filter field; no connector write method was added.

## Database indexes, migrations, environment or deployment changes

- No migration or environment change. The governorate query reads the existing normalized order JSON.

## Security, tenancy, money/time and idempotency review

- Query remains account-scoped and uses the typed filter allowlist with bound SQL parameters. Export state remains local-only. No money, time, connector direction, or job semantics changed.

## UI/PDF/XLSX evidence

- Updated orders and analytics Playwright baselines under `tests/e2e/*-snapshots/`.

## Known limitations and follow-up tasks

- Rich catalog-backed manual order entry, payment proof, shipping calculation, and split Woo/manual finance are tracked in T0902 and T0903.

## Handoff

- Branch: `fix/t0901-repair-orders-workspace-filters-export-visibility-an`
- Commit: see branch head
- Worktree: `D:\Projects\$current.projects\woo-ops-saas-worktrees\T0901-repair-orders-workspace-filters-export-visibility-an`
- Reviewer focus: RTL table scrolling, governorate JSON extraction, and local export-state labeling.
