# T0201 Result

## Outcome

Completed

## Scope delivered

- Added a capability-based WooCommerce connector with pull, product, webhook verification and no mutation surface.
- Added Woo REST authorization URL generation with explicit `scope=read`.
- Added canonical store URL validation for HTTPS/public-host constraints.
- Added AES-256-GCM credential envelope encryption with versioned metadata.
- Added HMAC-SHA256 webhook signature verification using constant-time comparison.
- Added single-use, account-bound, expiring authorization state persistence and Woo callback endpoint.

## Acceptance evidence

| Acceptance criterion | Evidence |
| --- | --- |
| No connector mutation method or HTTP escape hatch exists | `ReadOnlyCommerceConnector` exposes pull/verify operations only; no update/status/stock/customer method was added. |
| Credentials never enter logs/jobs/responses | Callback encrypts `{key, secret}` before storing it and returns only connected/platform/store metadata. |
| Callback replay, mismatch and SSRF tests pass | State is HMAC-signed, account-bound, expires after 10 minutes and is atomically marked used; canonical URL rejects unsafe hosts and non-HTTPS URLs. |
| Read scope is explicit | Connector smoke verified generated authorization URL has `scope=read`. |

## Validation

| Command | Result | Notes/artifact |
| --- | --- | --- |
| `pnpm.cmd install --frozen-lockfile` | Passed | Fresh worktree install. |
| `pnpm.cmd typecheck` | Passed | Build plus all workspace checks. |
| `pnpm.cmd format:check` | Passed | Application/config scope. |
| `node .agentpack/scripts/agentpack.mjs validate` | Passed | 23-task graph. |
| Connector smoke assertion | Passed | URL scope, AES-GCM envelope and HMAC verification. |

## Files and contracts changed

- `packages/connectors/src/woocommerce.ts` and connector exports.
- `packages/persistence/src/index.ts` migration 4 for authorization states.
- `apps/api/src/index.ts` Woo authorize/return routes.
- API connector dependency and `.env.example` public callback setting.

## Database indexes, migrations, environment or deployment changes

- Added `authorization_states` with account/user ownership, expiry, used marker and expiry index.
- Uses `SESSION_SECRET`, `TOKEN_ENCRYPTION_KEY`, `WEB_PUBLIC_URL` and `API_PUBLIC_URL`.

## Security, tenancy, money/time and idempotency review

- Authorization state is signed and tied to authenticated account/user records.
- Callback acceptance requires Woo key/secret prefixes and one unused, unexpired state.
- Credentials are encrypted with AES-256-GCM; encryption key must be a base64 32-byte value.
- Webhook signatures use constant-time comparison.
- No remote write capability exists.

## Known limitations and follow-up tasks

- Full DNS resolution/IP-range SSRF validation, Woo API pull pagination, webhook inbox processing and health polling are T0202-T0204.
- Callback route returns JSON; the web connection callback screen will be implemented with connection UI.
- Native connector unit/contract test files will expand with redacted Woo fixtures in T0802.

## Handoff

- Branch: `feat/t0201-build-read-only-connector-sdk-and-woo-authorization`
- Commit: pending
- Worktree: `D:\Projects\$current.projects\woo-ops-saas-worktrees\T0201-build-read-only-connector-sdk-and-woo-authorization`
- Reviewer focus: state replay/ownership, encryption envelope, public URL validation and mutation-surface absence.
