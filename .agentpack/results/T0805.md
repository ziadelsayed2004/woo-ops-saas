# T0805 Result

## Outcome

Completed

## Scope delivered

- Made Agentpack doctor select `pnpm.cmd` on Windows and `pnpm` elsewhere.
- Extended the shared process runner with an explicit opt-in `shell` option so Windows `.cmd`
  execution works without changing the default behavior of Git or other commands.
- Preserved all existing doctor output, validation, task-state, lock, and worktree semantics.

## Acceptance evidence

| Acceptance criterion | Evidence |
| --- | --- |
| Agentpack doctor reports pnpm available on Windows when pnpm.cmd is installed | `node .agentpack/scripts/agentpack.mjs doctor` passed `pnpm available: 10.15.0` on Windows, alongside Node, Git, repository, base branch, and pack checks. |
| Agentpack validation and the complete task board remain valid without changing task state semantics | `node .agentpack/scripts/agentpack.mjs validate` passed with 27 tasks; task state was changed only through the normal `task complete` command after evidence. |

## Validation

| Command | Result | Notes/artifact |
| --- | --- | --- |
| `pnpm.cmd install --frozen-lockfile` | PASS | Worktree dependencies installed from the existing lockfile. |
| `node .agentpack/scripts/agentpack.mjs doctor` | PASS | All checks passed; Windows package-manager resolution reports pnpm 10.15.0. |
| `node .agentpack/scripts/agentpack.mjs validate` | PASS | Agent pack valid: 27 tasks, 20 required files. |
| `git diff --check` | PASS | No whitespace errors. |

## Files and contracts changed

- `.agentpack/scripts/agentpack.mjs`: platform-aware doctor package-manager command and explicit
  Windows shell opt-in.
- `.agentpack/scripts/lib.mjs`: optional `shell` process-runner setting, defaulting to false.

## Database indexes, migrations, environment or deployment changes

- None.

## Security, tenancy, money/time and idempotency review

- No product runtime, account, connector, database, file, money, time, or job behavior changed.
- Shell execution is opt-in and used only for the static `pnpm.cmd --version` doctor probe; all other
  process calls retain the non-shell default.
- No user-controlled input is passed to the doctor probe.

## UI/PDF/XLSX evidence

- Not applicable; this is an Agentpack portability repair.

## Known limitations and follow-up tasks

- Windows Node emits its standard non-failing child-process shell deprecation warning for the static
  `.cmd` probe. The command is fixed and contains no user-provided arguments.
- None for this task.

## Handoff

- Branch: `chore/t0805-repair-agentpack-doctor-package-manager-detection-on`
- Commit: `b745bf4` (implementation and evidence)
- Worktree: `D:\Projects\$current.projects\woo-ops-saas-worktrees\T0805-repair-agentpack-doctor-package-manager-detection-on`
- Reviewer focus: verify Windows `.cmd` resolution is limited to doctor, shell remains opt-in, and
  non-Windows uses the original `pnpm` executable path.
