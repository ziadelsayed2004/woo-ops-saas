# T0907 Result

## Outcome

Completed

## Scope delivered

- Added shared runtime-path resolution for the API and database CLI.
- Hostinger production releases under `hbuilds` now resolve to one domain-level `.woo-ops-data` root instead of release-local `./data`.
- Explicit environment paths retain precedence; health diagnostics identify configured, Hostinger-domain, or release-local persistence without exposing paths.
- Extended the Hostinger smoke test to register an owner in one simulated release, start another release, and authenticate the same owner.

## Acceptance evidence

| Acceptance criterion | Evidence |
| --- | --- |
| Different hbuild releases share SQLite/private roots | Runtime-path unit test and Hostinger redeploy smoke pass. |
| Explicit environment paths retain precedence | Unit test verifies data and database overrides. |
| Safe health persistence diagnostic | `/health` returns only persistence mode and durable boolean, never a path. |
| Account survives a release switch | `redeployIdentityPersistence: true`; registration is closed and login succeeds in release B. |

## Validation

| Command | Result | Notes/artifact |
| --- | --- | --- |
| `npm run agent:validate` | Pass | Agent pack valid. |
| `npm run format:check` | Pass | Prettier clean. |
| `npm run lint` | Pass | All workspaces. |
| `npm run typecheck` | Pass | All workspaces. |
| `npm run test:unit` | Pass | Includes runtime persistence resolution. |
| `npm run test:integration` | Pass | Persistence 49/49 and API 23/23. |
| `npm run test:e2e:critical` | Pass | API 9/9; critical journey repeated twice. |
| `npm run test:hostinger` | Pass | Health, backup/restore, and cross-release owner login. |
| `npm run build` | Pass | Production bundles generated. |
| `git diff --check` | Pass | No whitespace errors. |

## Files and contracts changed

- `apps/api/src/runtime-paths.ts`: shared stable path policy.
- `apps/api/src/index.ts`, `apps/api/src/db-cli.ts`: shared policy consumers.
- `packages/contracts/src/index.ts`: safe persistence health diagnostic.
- `tools/hostinger-smoke.mjs`: cross-release identity regression.
- Deployment documentation and environment example.

## Database indexes, migrations, environment or deployment changes

- No schema migration.
- On Hostinger, remove release-relative `WOO_OPS_DATA_DIR=./data` and `WOO_OPS_DATABASE=./data/woo-ops.sqlite` values to use automatic domain-level persistence, or replace them with absolute persistent paths outside `hbuilds` and `public_html`.

## Security, tenancy, money/time and idempotency review

- No account, connector, money, or remote-write contract changed.
- Health exposes no filesystem path or PII.
- Existing database/account isolation remains covered by integration and critical E2E tests.

## UI/PDF/XLSX evidence

- Not applicable.

## Known limitations and follow-up tasks

- Existing account data in an already-discarded Hostinger release cannot be reconstructed without a retained release directory or backup.

## Handoff

- Branch: `fix/t0907-persist-production-identity-and-data-across-hostinge`
- Commit: recorded after task completion
- Worktree: `D:\Projects\$current.projects\woo-ops-saas-worktrees\T0907-persist-production-identity-and-data-across-hostinge`
- Reviewer focus: Hostinger ancestor detection, explicit override precedence, safe diagnostics, and cross-release login.
