# T0204 Result

## Outcome

Completed

## Scope delivered

- Added read-only Woo order/refund pagination with resumable page cursors.
- Added deterministic order normalization for money minor units, UTC dates, customer/billing/shipping data, line items, and refunds.
- Added SQLite migration v7 for remote order payloads, normalized snapshots, refunds, sync runs, deletion markers, and stale-export timestamps.
- Added remote-owned upsert logic that preserves local status/export state and marks changed exported orders stale.

## Acceptance evidence

| Acceptance criterion | Evidence |
| --- | --- |
| Initial sync resumes and is idempotent | `pullRemote` accepts `startPage`; account/connection/external-order uniqueness and upsert tests pass without duplicate orders/refunds. |
| Webhook replay creates one effect | Existing webhook inbox uniqueness from T0202 remains in the migrated schema; normalized order upsert uses the same external identity and is idempotent. |
| Remote updates preserve local fields and mark stale exports | Persistence test updates a local packed/exported order, applies a remote update, and verifies local fields remain unchanged while `stale_export_at` is set. |

## Validation

| Command | Result | Notes/artifact |
| --- | --- | --- |
| `pnpm.cmd install --frozen-lockfile` | PASS | Dependencies installed from the existing lockfile. |
| `pnpm.cmd typecheck` | PASS | Workspace build and TypeScript checks pass. |
| `pnpm.cmd test:contract --filter woocommerce-orders` | PASS | 4 connector contract tests and 6 persistence tests passed. |
| `pnpm.cmd test:integration --filter order-sync` | PASS | 6 persistence tests passed. |
| `pnpm.cmd test:chaos --filter webhook-replay` | PASS | 6 persistence replay/idempotency tests passed. |
| `pnpm.cmd exec prettier --check <affected files>` | PASS | All T0204 files formatted. |
| `node .agentpack/scripts/agentpack.mjs validate` | PASS | Agent pack valid. |

## Files and contracts changed

- `packages/connectors/src/index.ts`
- `packages/connectors/src/woocommerce.ts`
- `packages/connectors/tests/woo-orders.test.js`
- `packages/persistence/src/index.ts`
- `packages/persistence/tests/orders.test.js`

## Database indexes, migrations, environment or deployment changes

- Migration v7 adds normalized order payload columns, `order_refunds`, `order_sync_runs`, and indexes.
- No remote mutation methods or new production credentials were introduced.

## Security, tenancy, money/time and idempotency review

- Connector remains read-only and only calls `/wp-json/wc/v3` with bounded page sizes.
- Money is parsed from Woo decimal strings into integer minor-unit strings; no floating-point arithmetic is used.
- Timestamps normalize to UTC ISO values.
- Repository upserts and refund writes require account context and connection identity; local-owned fields are excluded from update assignments.
- Source JSON is retained as a snapshot and source hash is deterministic for replay comparison.

## UI/PDF/XLSX evidence

- Not applicable to order synchronization foundation.

## Known limitations and follow-up tasks

- A later job orchestration task must connect polling/webhook jobs to `pullRemote`, persist per-page `order_sync_runs`, and handle remote deletion reconciliation.
- Webhook inbox processing remains the durable intake boundary from T0202; this task does not add remote writes.

## Handoff

- Branch: `feat/t0204-sync-and-normalize-woo-orders-refunds-and-webhooks`
- Commit: `eb83fcf` (amended with final evidence metadata)
- Worktree: `D:\Projects\$current.projects\woo-ops-saas-worktrees\T0204-sync-and-normalize-woo-orders-refunds-and-webhooks`
- Reviewer focus: remote/local merge allowlist, exact minor-unit conversion, UTC handling, and idempotent refund identity.
