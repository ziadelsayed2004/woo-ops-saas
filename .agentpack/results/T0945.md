# T0945 Result

## Outcome

Completed

## Scope delivered

- Persisted the Excel or invoices-and-print history tab in the `/exports` URL so a refresh restores the same view.
- Recycled the shared Chromium render session every 50 generated documents to bound browser memory during large batches.
- Classified browser startup, connection loss, crashed-page and closed-session failures as renderer outages.
- Kept the currently claimed document recoverable during a transient renderer outage and allowed the durable job retry to resume it without failing every remaining order.
- Preserved completed order PDFs across retries and exposed localized terminal renderer failure details with the existing retry control.

## Acceptance evidence

| Acceptance criterion | Evidence |
| --- | --- |
| Refresh preserves the selected history tab | Playwright switches to invoices-and-print, verifies `?history=documents`, reloads, then repeats for Excel. |
| Large batches recycle browser resources | Integration test sets a one-document bound and verifies two successful sessions are opened and closed for two documents. Production bound is 50. |
| Transient renderer outage is recoverable | Integration test injects a launch failure, verifies no item becomes failed, resumes the same batch, and completes both documents. |
| Per-order failures remain isolated | Existing document unit and persistence integration suites continue to pass their partial-batch isolation scenarios. |
| Failure detail remains actionable | Failed document rows show localized renderer guidance while retaining Show files and Retry controls. |

## Validation

| Command | Result | Notes/artifact |
| --- | --- | --- |
| `npm run agent:validate` | Passed | 88 tasks and 20 required files valid. |
| `npm run format:check` | Passed | All matched files formatted. |
| `npm run lint` | Passed | All workspaces. |
| `npm run typecheck` | Passed | All workspaces. |
| `npm run test:unit` | Passed | All workspace unit suites. |
| `npm run test:integration` | Passed | Persistence 53/53 and API 27/27, including the new renderer recovery test. |
| `npm run test:e2e -- --grep exports` | Passed | UI 7/7 and API E2E 9/9. |
| `npm run build` | Passed | All workspaces; existing Vite chunk-size advisory only. |
| `git diff --check` | Passed | No whitespace errors. |

## Files and contracts changed

- The exports workspace recognizes `history=documents`; Excel remains the clean `/exports` default.
- The document effect accepts injectable renderer options for deterministic recovery and recycling tests.
- Document batch summaries display their bounded failure code as localized operator guidance.

## Database indexes, migrations, environment or deployment changes

- None.

## Security, tenancy, money/time and idempotency review

- All artifacts and batch state remain account scoped and checksum protected.
- Existing immutable snapshots and idempotent artifact identifiers are preserved during retries.
- Browser errors are reduced to bounded safe codes instead of exposing raw process details.
- No remote WooCommerce mutation behavior was added.

## UI/PDF/XLSX evidence

- `tests/e2e/exports.spec.ts` verifies both persisted export-history tabs after real browser reloads.
- `apps/api/tests/document-jobs.integration.test.js` verifies renderer recovery and bounded session recycling with valid generated PDF bytes.

## Known limitations and follow-up tasks

- None.

## Handoff

- Branch: `fix/t0945-persist-export-history-tabs-and-harden-large-documen`
- Commit: `fix(exports): T0945 persist tabs and harden document jobs`
- Worktree: `D:\Projects\$current.projects\woo-ops-saas-worktrees\T0945-persist-export-history-tabs-and-harden-large-documen`
- Reviewer focus: URL-backed tab restoration, retryable renderer-outage path, and browser-session recycling.
