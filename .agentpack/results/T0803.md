# T0803 Result

## Outcome

Completed

## Scope delivered

- Added a deterministic fixture-only critical journey covering registration, login, Woo read-only
  normalization/sync, order search/filter, export selection and CSV/exported state, manual orders,
  invoice/thermal documents, analytics rebuild/query, and backup/restore.
- Added a repeatable critical E2E runner that builds the monorepo, runs the API E2E suite, and repeats
  the full journey in isolated temporary SQLite/data directories.
- Added 10k/100k-order SQLite performance fixtures with p95 budgets for list, typed filtering, search,
  cross-page selection, and durable job round trips. Reports are written to the ignored
  `artifacts/performance/T0803.json` path.
- Added a production-style Hostinger smoke runner for the single Node process, `/health`, Vite web
  shell serving, backup listing, and restore dry-run.
- Documented Hostinger topology, environment, capacity baseline, Cron maintenance, rollback, support,
  and release gates; wired the release checks into CI.

## Acceptance evidence

| Acceptance criterion | Evidence |
| --- | --- |
| Critical journey passes repeatedly without production data | `pnpm.cmd test:e2e:critical`: build passed, API E2E suite passed 5/5, and the complete fixture journey passed repeat 1/2 and 2/2. The journey uses isolated temporary SQLite data and a GET-only Woo fixture connector. |
| Performance budgets and capacity limits are documented | `pnpm.cmd test:performance` passed 25 warm iterations at both 10,000 and 100,000 orders. At 100k p95: list 79.97 ms, filtered list 87.98 ms, search 126.57 ms, selection 80.74 ms, job round trip 0.41 ms; all are below 700/700/1500/700/250 ms budgets. Documentation is in `.agentpack/architecture/DEPLOYMENT.md` and `TESTING.md`. |
| Hostinger build/start/health/backup smoke passes | `pnpm.cmd test:hostinger` passed with `node apps/api/dist/index.js`; health reported `ok`/`connected`, the `/` response was HTML from the Vite build, backup creation/listing passed, and restore dry-run returned `dryRun: true`, `restored: false`. |

## Validation

| Command | Result | Notes/artifact |
| --- | --- | --- |
| `pnpm.cmd install --frozen-lockfile` | PASS | Lockfile install completed. |
| `pnpm.cmd test:e2e:critical` | PASS | 5 API E2E tests plus 2 repeated critical journey runs; runner also passed a full build. |
| `pnpm.cmd test:performance` | PASS | 10k/100k datasets, 25 iterations, all p95 budgets passed; report at ignored `artifacts/performance/T0803.json`. |
| `pnpm.cmd test:hostinger` | PASS | Production entrypoint, health, static web shell, backup list, and restore dry-run. |
| `pnpm.cmd build` | PASS | All 12 workspace build targets passed. Vite emitted only its existing large-chunk advisory for the 539.24 kB client bundle. |
| `pnpm.cmd format:check` | PASS | All matched files use Prettier style. |
| `pnpm.cmd lint` | PASS | All 12 workspace lint/type boundary checks passed. |
| `pnpm.cmd typecheck` | PASS | Workspace build and no-emit TypeScript checks passed. |
| `pnpm.cmd test:unit` | PASS | Unit suites passed, including persistence 29, connectors 8, exports 4, documents 5, API 2, analytics 3. |
| `pnpm.cmd test:integration` | PASS | Persistence 29 and API 5 integration tests passed. |
| `pnpm.cmd test:contract` | PASS | Connectors 8 and persistence 29 contract tests passed. |
| `pnpm.cmd test:golden` | PASS | Exports 4 and documents 5 golden/physical-dimension tests passed. |
| `pnpm.cmd test:security` | PASS | Connector 4, persistence 29, and API 2 security tests passed. |
| `pnpm.cmd test:tenant-isolation` | PASS | Persistence 2 and API 1 account-isolation tests passed. |
| `pnpm.cmd security:scan` | PASS | 162 tracked text files inspected; no secrets found. |
| `pnpm.cmd security:audit` | PASS | No known production vulnerabilities. |
| `node .agentpack/scripts/agentpack.mjs validate` | PASS | Agent pack valid: 25 tasks, 20 required files. |
| `git diff --check` | PASS | No whitespace errors. |

## Files and contracts changed

- `apps/api/src/index.ts`: serves the built Vite application from the configured/known dist paths while
  preserving API and health routing.
- `apps/api/tests/critical.e2e.test.js`: complete local operator journey and restore assertions.
- `tools/run-critical-e2e.mjs`, `tools/performance-test.mjs`, `tools/hostinger-smoke.mjs`: release,
  performance, and Hostinger smoke runners.
- `package.json`: release gate scripts.
- `.agentpack/architecture/DEPLOYMENT.md`, `.agentpack/architecture/TESTING.md`, `README.md`: runbook,
  capacity, and verification documentation.
- `.github/workflows/ci.yml`: build, critical E2E, performance, and Hostinger smoke gates.

## Database indexes, migrations, environment or deployment changes

- No schema or migration changes.
- The API now resolves `WOO_OPS_WEB_DIST_DIR` or the standard monorepo/API-relative web dist paths and
  serves the web shell from the same Node process.
- Hostinger deployment uses `node apps/api/dist/index.js`, a private `WOO_OPS_DATA_DIR`, configured
  secrets, and the documented migration/backup/restore commands.

## Security, tenancy, money/time and idempotency review

- The journey uses authenticated account context and verifies local manual-order invariants; no client
  account identifier is accepted as authority.
- Woo fixture transport asserts GET-only/manual redirects and uses a public test address; no remote
  mutation is introduced.
- Temporary databases and private temporary file directories are isolated per run and cleaned up.
- Performance job payloads remain small and use the durable account-scoped job lifecycle.
- Existing integer minor-unit, UTC, export idempotency, and backup checksum rules are exercised by the
  critical and repository-wide tests.

## UI/PDF/XLSX evidence

- The critical journey generated and downloaded an A4 invoice and an 80mm thermal PDF and verified the
  response checksum and non-empty bytes.
- The journey generated a CSV export and verified the bytes survived backup/restore.
- `pnpm.cmd test:golden` passed XLSX/CSV assertions and A4/thermal/label document dimension and
  reproducibility tests.
- Hostinger smoke verified the built Vite HTML shell is served by the production Node process.

## Known limitations and follow-up tasks

- Live WooCommerce credentials, a real Hostinger account, DNS/TLS, and carrier/legal invoice approval
  remain external release inputs; this task deliberately certifies deterministic fixtures and local
  deployment behavior only.
- Vite reports a non-failing large client chunk advisory; code splitting can be a future performance
  follow-up if the selected hosting tier or browser budget requires it.
- No new task is required for the accepted T0803 scope.

## Handoff

- Branch: `ops/t0803-run-critical-e2e-performance-and-hostinger-release-g`
- Commit: `aafac86` (implementation and evidence)
- Worktree: `D:\Projects\$current.projects\woo-ops-saas-worktrees\T0803-run-critical-e2e-performance-and-hostinger-release-g`
- Reviewer focus: verify the full release runner uses isolated fixtures, static serving does not expose
  private data, performance budgets match the documented operations, and Hostinger paths remain valid
  when the application root differs.
