# T0923 Result

## Outcome

Completed locally; deployment to the live Hostinger application remains a separate operator action.

## Scope delivered

- Fixed the invalid export filename template (`{format}` is mandatory), and surfaced the API's actual error code for profile, version, and batch creation instead of a blanket `EXPORT_CREATE_FAILED`.
- Matched the supplied Woo order workbook's 38 ordered columns, adding readable billing and shipping governorate columns alongside the source state codes. Each order line receives a row; money is converted from integer minor units without floating-point arithmetic.
- Embedded export and PDF job history, progress, retry, and repeat download in Orders. Removed the standalone Exports navigation and mapped its old route to Orders.
- Included governorate, payment, shipping, item name/quantity/line amount and correctly scaled total in A4 invoices and shipping labels.
- Preserved Woo `customer_note` in normalized order snapshots for the workbook field.

## Acceptance evidence

| Acceptance criterion | Evidence |
| --- | --- |
| Creation succeeds or reports precise error | Browser export journey verifies valid profile version and batch creation; API error code is parsed on failures. |
| Woo workbook layout | 40 ordered column mappings; XLSX unit test checks line rows, code/name governorates, headers, and money. |
| History and repeat download within Orders | Browser test verifies completed batch link on the order screen. |
| A4 and shipping label order contents | Document renderer outputs order line names, quantities, amounts, method and total; existing PDF generation/page-size tests pass. |

## Validation

| Command | Result |
| --- | --- |
| `npm.cmd run agent:validate` | Passed |
| `npm.cmd run format:check` | Passed after formatting |
| `npm.cmd run lint` | Passed |
| `npm.cmd run typecheck` | Passed |
| `npm.cmd run test:unit` | Passed |
| `npm.cmd run test:integration` | Passed |
| `npm.cmd run test:contract` | Passed |
| `npm.cmd run test:e2e` | 26 browser and 9 API tests passed |
| `npm.cmd run build` | Passed |
| `git diff --check` | Passed |

## Files and contracts changed

- Web orders/export workflow and Woo column manifest, export engine, Woo normalization and PDF renderer; associated unit/browser tests and three reviewed visual baselines.
- No API route or schema migration. Existing private export batch/download APIs remain account scoped.

## Security, tenancy, money/time and idempotency review

- No remote mutation or client-supplied account identity introduced. Export snapshots and existing private download authorization are unchanged.
- Amount formatting uses `BigInt` minor units; client creation keeps unique idempotency keys.

## UI/PDF/XLSX evidence

- Browser journey verifies the 40-column version payload and in-page repeat download URL.
- XLSX test loads the generated workbook and checks governorate, numeric formatting, line rows and formula sanitization.
- PDF physical page and Arabic font generation tests pass. Visual baselines updated only for removal of Exports navigation and the embedded Orders history.

## Known limitations and follow-up tasks

- Live Woo/Hostinger export was not exercised here; deployment and an authenticated smoke test with the production account are required.
- A fixed-size shipping label cannot fit an arbitrarily long order; the current renderer truncates after available space. A multi-page packing-list design would be separate work.

## Handoff

- Branch: `fix/t0923-match-woo-order-sheet-and-keep-export-history-in-ord`
- Worktree: `D:\Projects\$current.projects\woo-ops-saas-worktrees\T0923-match-woo-order-sheet-and-keep-export-history-in-ord`
- Reviewer focus: Woo workbook parity, existing export versions, and document density for long orders.
