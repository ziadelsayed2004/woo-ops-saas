# T0942 Result

## Outcome

Completed

## Scope delivered

- Captured the real operating-system temporary directory once, before portable browser preparation mutates `TMPDIR`, `TMP` and `TEMP`.
- Made the postinstall prepare-then-launch sequence idempotent while retaining rejection of actual system-temp cache paths.
- Updated the Linux-launch regression to mutate the real process environment, prepare twice, and restore the environment after the test.

## Acceptance evidence

| Acceptance criterion | Evidence |
| --- | --- |
| Repeated preparation succeeds | The document test now prepares with `process.env`, then calls the Linux launch branch which prepares again; all 17 document tests pass. |
| System temp remains rejected | The original system temp is captured at module initialization and the existing unsafe-path assertion passes after process temp variables are mutated. |
| Hostinger sequence proceeds | A real Sparticuz extraction using prepare then Linux launch returned the same application cache and `.cache/woo-ops-browser/chromium` executable without `WOO_OPS_BROWSER_CACHE_DIR_UNSAFE`. |
| Required checks pass | Validation, formatting, lint, typecheck, unit, golden, integration, browser installer smoke, build and diff checks passed. |

## Validation

| Command | Result | Notes/artifact |
| --- | --- | --- |
| `npm run agent:validate` | Pass | 85 tasks valid. |
| `npm run format:check` | Pass | All matched files formatted. |
| `npm run lint` | Pass | All workspaces. |
| `npm run typecheck` | Pass | All workspaces. |
| `npm run test:unit` | Pass | Includes exact repeated-preparation regression. |
| `npm run test:golden` | Pass | Document and export suites. |
| `npm run test:integration` | Pass | Persistence and API suites. |
| Real prepare + portable extraction check | Pass | Both cache and executable stayed under `.cache/woo-ops-browser`. |
| `node tools/install-document-browser.mjs` | Pass | Local browser readiness PDF succeeded. |
| `npm run build` | Pass | All workspaces. |
| `git diff --check` | Pass | Clean. |

## Files and contracts changed

- `packages/documents/runtime/browser.mjs`: stable original system-temp reference.
- `packages/documents/tests/documents.test.js`: exact postinstall double-preparation regression.

## Database indexes, migrations, environment or deployment changes

- None.

## Security, tenancy, money/time and idempotency review

- Unsafe real system-temp paths remain rejected; no account, connector, persistence, money or job behavior changed.

## UI/PDF/XLSX evidence

- No visual changes; existing document golden tests pass.

## Known limitations and follow-up tasks

- Live Linux process launch is confirmed by the next Hostinger redeploy; the reported cache-stage failure is reproduced and fixed locally with the real package extraction path.

## Handoff

- Branch: `fix/t0942-make-hostinger-chromium-cache-preparation-idempotent`
- Commit: pending
- Worktree: `D:\Projects\$current.projects\woo-ops-saas-worktrees\T0942-make-hostinger-chromium-cache-preparation-idempotent`
- Reviewer focus: stable system-temp capture and process environment restoration in regression coverage.
