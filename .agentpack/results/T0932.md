# T0932 Result — Woo export status badges

## Outcome

- Replaced plain Woo export status text in the orders grid with compact Material UI status chips.
- Exported uses a filled success chip, not exported uses a neutral outlined chip, and unavailable uses an outlined warning chip.
- Kept the existing localized Arabic and English labels and added stable test identifiers.

## Validation evidence

- `npm run agent:validate` — passed.
- `npm run format:check` — passed.
- `npm run lint` — passed.
- `npm run typecheck` — passed.
- `npm run test:e2e -- --grep "Woo export status"` — passed; browser badge assertions and API E2E passed.
- `npm run build` — passed.
- `git diff --check` — passed.

## Security and data direction

- Presentation-only change; no connector, persistence, tenant scope, or WooCommerce write behavior changed.
