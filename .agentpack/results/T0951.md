# T0951 Result

## Outcome

Completed for the scoped order overlays. A manual-order visual baseline mismatch outside the changed components is recorded below.

## Scope delivered

- Replaced location-derived CSS names in the export dialog and order detail drawer with semantic component classes.
- Corrected portal-mounted MUI styling by targeting the dialog and drawer papers directly instead of `#root` descendants. Removed the drawer's inline `sx` and the dialog's inline radius.
- Added consistent card spacing, responsive actions, 12px dialog radius, wrapping badges, aligned fact rows and narrow-screen drawer sizing.
- Added an E2E geometry regression for Arabic and English, desktop and mobile.

## Acceptance evidence

| Acceptance criterion | Evidence |
| --- | --- |
| Export actions do not overlap and are responsive | `tests/e2e/orders.spec.ts` geometry test verifies desktop row separation and mobile stacking; `output/ui/output-dialog-download.png` and `output/ui/output-dialog-print.png` show the corrected layout. |
| Drawer badges, tabs and facts remain readable in RTL/LTR | Geometry test verifies badge direction, fact grid, mobile width, and both paper directions. |
| Portal CSS applies and classes are semantic | Dialog and drawer paper classes are styled without `#root`; geometry test checks computed layout. |
| Affected quality checks pass | 11/11 `orders.spec.ts` tests and 8/8 export-grep UI tests pass; unit, format, lint, typecheck, build and style guard pass. |

## Validation

| Command | Result | Notes/artifact |
| --- | --- | --- |
| `npm.cmd run agent:validate` | Pass | 94 tasks valid. |
| `npm.cmd run format:check` | Pass | Prettier clean. |
| `npm.cmd run lint` | Pass | Includes workspace build. |
| `npm.cmd run typecheck` | Pass | All workspaces. |
| `npm.cmd run test:unit` | Pass | All workspace unit suites. |
| `npm.cmd run test:e2e -- --grep exports` | Pass | 8 UI tests and 9 API E2E tests. |
| `npx.cmd playwright test tests/e2e/orders.spec.ts` | Pass | 11 UI tests, including the new layout regression. |
| `npm.cmd run test:e2e -- --grep orders` | Partial | 14 passed; 2 manual-order screenshot snapshots differ in form height by 28px. Scoped order overlay tests passed. |
| `npm.cmd run build` | Pass | Executed within lint and exports E2E workflow. |
| `npm.cmd run styles:check` | Pass | 5 TSX files satisfy the web style guard. |
| `git diff --check` | Pass | No whitespace errors. |

## Files and contracts changed

- `apps/web/src/OrderOutputDialog.tsx`, `.css`: semantic classes and responsive dialog layout.
- `apps/web/src/App.tsx`, `.css`: semantic detail classes, responsive drawer and fact layout.
- `tests/e2e/orders.spec.ts`: layout regression; `output/ui/output-dialog-*.png`: visual evidence.
- No API, database or data contract changed.

## Database indexes, migrations, environment or deployment changes

- None.

## Security, tenancy, money/time and idempotency review

- Presentation-only changes. No connector writes, account queries, money calculations, dates, persistence, or job behavior changed.

## UI/PDF/XLSX evidence

- `output/ui/output-dialog-download.png` and `output/ui/output-dialog-print.png`; no PDF or XLSX generation changed.

## Known limitations and follow-up tasks

- The broad `--grep orders` command also selects `manual-orders.spec.ts` by substring. Its two full-page visual baselines differ by 28px on this Windows run; the form and its styles are outside T0951. Do not update those goldens without a separate review.
- Windows `npm.cmd` treats the pipe in the literal `--grep "orders|exports"` as a shell pipe; the selectors were run separately.

## Handoff

- Branch: `fix/t0951-repair-order-overlays-and-semantic-css-layout`
- Commit: `837d11d`
- Worktree: `D:\Projects\$current.projects\woo-ops-saas-worktrees\T0951-repair-order-overlays-and-semantic-css-layout`
- Reviewer focus: Portal styling specificity, RTL/LTR alignment, and mobile button wrapping.
