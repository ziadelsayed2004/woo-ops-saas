# T0931 Result — Production-safe PDF rendering

## Outcome

- Kept Chromium HTML-to-PDF as the preferred renderer.
- Added a deterministic pdf-lib fallback when Chromium is absent or cannot launch on Hostinger.
- Added an explicit portable-renderer test path covering A4, 80mm receipt, and 80mm shipping roll.
- Made thermal and shipping-roll fallback heights content-driven and bounded to 100–500mm.
- Preserved Arabic-capable embedded fonts, order barcode, store QR, support phone, and support email.
- Redacted unexpected renderer failures before persisting per-order job errors.

## Validation evidence

- `npm run agent:validate` — passed (74 tasks, 20 required files).
- `npm run format:check` — passed.
- `npm run lint` — passed.
- `npm run typecheck` — passed.
- `npm run test:unit` — passed, including 9/9 document tests.
- `npm run test:integration` — passed (53 persistence + 26 API tests).
- `npm run test:security` — passed (7 connector + 53 persistence + 5 API tests).
- `npm run test:golden` — passed (9 document + 8 export tests).
- `npm run build` — passed.
- `git diff --check` — passed.

## Security and tenancy

- No connector or remote WooCommerce mutation was added.
- Document snapshots and private artifact storage remain account-scoped.
- Browser launch exceptions are reduced to bounded codes; filesystem paths and host details are not persisted.

## Deployment note

After deploying the commit, retry the failed document batches. Existing failed rows remain immutable history; their retry generates new private artifacts through the repaired renderer.
