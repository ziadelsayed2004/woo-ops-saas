# T0301 Result

## Outcome

Completed

## Scope delivered

- Added a canonical order query read model with server-side bounded pagination.
- Added allowlisted typed filter compilation for AND/OR groups, text, enum, date, and minor-unit numeric operators.
- Added safe search across order identity/status/currency/normalized fields and stable cursor sorting.
- Added account-scoped query tests and security rejection tests for unsafe fields/operators/values.

## Acceptance evidence

| Acceptance criterion | Evidence |
| --- | --- |
| No raw SQL/operator/path can enter queries | `compileFilter` maps only server-owned fields/operators to fixed SQL fragments; malicious field/operator/object-value cases are rejected in query security tests. |
| All list APIs have bounded limits and stable sort | `queryOrders` caps limits at 100, validates sort fields/direction, uses deterministic sort plus `id` tie-breaker, and emits opaque validated cursors. |
| Search covers canonical and mapped fields | Search covers order number, external ID, remote/local status, currency, and normalized snapshot JSON; query tests verify order-number search and typed filters. |

## Validation

| Command | Result | Notes/artifact |
| --- | --- | --- |
| `pnpm.cmd install --frozen-lockfile` | PASS | Existing lockfile installed successfully. |
| `pnpm.cmd typecheck` | PASS | Workspace build and TypeScript checks pass. |
| `pnpm.cmd test:unit --filter order-query` | PASS | 8 persistence tests passed. |
| `pnpm.cmd test:integration --filter order-repository` | PASS | 8 persistence tests passed. |
| `pnpm.cmd test:security --filter query-input` | PASS | 8 persistence/security tests passed. |
| `pnpm.cmd exec prettier --check <affected files>` | PASS | All T0301 files formatted. |
| `node .agentpack/scripts/agentpack.mjs validate` | PASS | Agent pack valid. |

## Files and contracts changed

- `packages/persistence/src/index.ts`
- `packages/persistence/tests/query.test.js`
- `package.json`, `packages/persistence/package.json`

## Database indexes, migrations, environment or deployment changes

- Query uses existing order indexes from prior migrations; no new migration was required.
- Added root/persistence security test script wiring for the task gate.

## Security, tenancy, money/time and idempotency review

- Every query begins with authenticated account context; client-provided account IDs are not accepted as query input.
- Field names, operators, sort columns, limits, cursors, and values are runtime-validated; values are bound parameters.
- Money comparisons use stored minor-unit strings and no floating-point arithmetic.
- Cursor payload is opaque base64url JSON containing only server-generated sort value and ID.
- No remote connector mutation or sensitive data logging was introduced.

## UI/PDF/XLSX evidence

- Not applicable to query foundation.

## Known limitations and follow-up tasks

- The HTTP `/orders/query` controller and full UI filter builder are owned by subsequent order workspace tasks.
- Current canonical `remoteCreatedAt` query field maps to the available `remote_modified_at` storage field until a dedicated source-created timestamp migration is introduced.

## Handoff

- Branch: `feat/t0301-implement-canonical-order-query-and-typed-filter-ast`
- Commit: `3b3fb65` (amended with final evidence metadata)
- Worktree: `D:\Projects\$current.projects\woo-ops-saas-worktrees\T0301-implement-canonical-order-query-and-typed-filter-ast`
- Reviewer focus: filter allowlist, cursor boundary behavior, account scoping, and string minor-unit ordering.
